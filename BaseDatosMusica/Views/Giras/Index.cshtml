@model IEnumerable<BaseDatosMusica.Models.Gira>

@{
    ViewData["Title"] = "Index";
}

<h1>GIRAS</h1>

<p>
    <a asp-action="Create"><img src="~/img/añadircanciones.png" width="40px" height="40px" /></a>
</p>
<form asp-controller="Giras" asp-action="Index">
    <p>
        Gira: <input type="text" name="SearchString" />
        <input type="submit" value="Filtrar" />
    </p>
</form>

<!--<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.FechaInicio)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.FechaFinal)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.FechaInicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaFinal)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id"><img src="~/img/editar-removebg-preview.png" width="25px" height="25px" /></a>
                <a asp-action="Details" asp-route-id="@item.Id"><img src="~/img/detalles-removebg-preview.png" width="25px" height="25px" /></a> |
                <a asp-action="Delete" asp-route-id="@item.Id"><img src="~/img/borrar-removebg-preview.png" width="25px" height="25px" /></a>
            </td>
        </tr>
    }
    </tbody>
</table>
-->

<!-- Invocación del componente de vista -->
<div>
    @await Component.InvokeAsync("TourConcertComponent")
</div>